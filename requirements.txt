# DeySafe / SHIELD — the app core is Python standard library only.
# The one optional dependency below is used ONLY when DATABASE_URL is set
# (Postgres production mode). With no DATABASE_URL the app runs on stdlib SQLite.
psycopg2-binary>=2.9
